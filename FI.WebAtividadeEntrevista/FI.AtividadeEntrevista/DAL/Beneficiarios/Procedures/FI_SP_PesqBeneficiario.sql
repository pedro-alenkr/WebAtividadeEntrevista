CREATE PROC FI_SP_PesqBeneficiario
	@ID BIGINT
AS
BEGIN
	IF(ISNULL(@ID,0) = 0)
		SELECT NOME, IDCLIENTE, CPF FROM BENEFICIARIOS WITH(NOLOCK)
	ELSE
		SELECT NOME, IDCLIENTE, CPF FROM BENEFICIARIOS WITH(NOLOCK) WHERE IDCLIENTE = @ID
END